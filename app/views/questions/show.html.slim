h1= @question.title
p= @question.body

.answers
  = render 'answers'
    
= simple_form_for [@question, @answer], remote: true do |f|
  = f.label 'Answer'
  .answer-errors
  = f.input :body, label: ''
  = f.button :submit

= link_to 'Edit', edit_question_path(@question)
